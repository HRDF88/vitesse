package com.example.vitesse.ui.home.candidate

import android.os.Bundle
import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import androidx.fragment.app.Fragment
import com.example.vitesse.R
import dagger.hilt.android.AndroidEntryPoint

@AndroidEntryPoint
class CandidateFragment : Fragment() {
/*
    private var _binding : CandidateFragmentBinding? = null
    private val binding get() = _binding

    private val candidateAdapter = CandidateAdapter(emptyList())

    override fun onCreateView(
        inflater: LayoutInflater,
        container: ViewGroup?,
        savedInstanceState:Bundle?
        ): View {
        val rootView = inflater.inflate(R.layout.fragment_candidate, container,false)
        return binding.root
    }*/
}
